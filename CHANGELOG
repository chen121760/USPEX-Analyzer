# USPEX Analyzer

## 版本更新记录

### V1.4.3

四元体系全面支持 — 四面体相图与全链路适配

- **3D 四元四面体相图**：新增 `QuaternaryHullPlot3D` 组件，基于4D热力学下凸包（composition + energy 空间）计算真实四元相图的3D投影面
  - 凸包面分为外部包络面（蓝灰色，hull boundary）和内部分界面（橙红色，相邻四面体胞间共享面），直观展示相区边界
  - 四面体线框（灰色虚线） + 4个顶点元素标签 + 凸包边线框
  - 稳定相红色菱形标记，非稳相 Viridis 色带着色（按 fitness）
  - 支持 Camera 位置记忆（切换数据后重置）
  - `quaternaryToCartesian` 函数将四元素组分映射到正四面体3D坐标
- **类型系统扩展**：`SystemType` 新增 `'quaternary'`，`NComponentsFilterCondition.value` / `UnifiedCondition.value` 均适配 `1 | 2 | 3 | 4`
- **筛选功能**：DataTable 内联筛选与 FilterPage 独立筛选均增加"四元（4种元素）"选项，前端逻辑 `s.composition.filter(c>0).length === f.value` 天然支持1-4
- **i18n**：中英文翻译全覆盖 `table.filterQuaternary`、`system.quaternary`、`hull.quaternaryTitle`
- **Hull Workshop 提示**：四元体系不适用于 Hull Workshop 的凸包重建，显示兜底提示文案引导用户到 Convex Hull 页面查看四面体可视化

### V1.4.2

三元相图 3D 可视化 — 三维凸包交互

- **3D 三元相图**：新增 `TernaryHullPlot3D` 组件，使用 Plotly `scatter3d` + `mesh3d` 渲染完整三维凸包面，支持旋转（`dragmode: turntable`）、缩放、平移
- 凸包面灰色半透明渲染 + 平滑光照（ambient / diffuse / specular / fresnel）+ 黑色线框边，增强立体感与面片辨识度
- **2D / 3D 视图切换**：ConvexHullPage 标题栏右上角新增 `2D Projection` / `3D View` 按钮，切换项目时自动重置为 2D

Fitness 控制与色带优化

- **数值输入框**：二元、二维三元、三维三元相图的 Fitness 控制栏均新增数值输入框，支持手动输入精确值（与滑块联动）
- **动态色带范围**：色带 `cmax` 跟随当前 `fitnessMax` 滑块值动态缩放，过滤范围内颜色区分度更高

上传页面 — USPEX 25 兼容与交互优化

- **USPEX 25 格式支持**：新增 USPEX25 核心文件组（仅需 `Individuals` + `gatheredPOSCARS` 两个文件），与 USPEX 10.5 核心文件组（四个文件）并列展示
- 文件格式自动识别：拖入文件后根据内容特征（`Individuals` 列头、`gatheredPOSCARS` 标记格式）自动判断 USPEX 10.5 或 USPEX25，无需用户手动选择
- 文件组标签明确标注版本（`核心文件 — USPEX 10.5 必需` / `核心文件 — USPEX25 最低要求`），两组之间显示"自动识别：根据文件内容匹配对应版本"提示
- **Enter 快捷键**：文件上传完成后按回车键即可启动分析（输入框内不触发）
- **Start Analysis 按钮固定置底**：全宽横跨视口底部，始终可见，无需滚动到上传区底部
- 按钮下方提示文案随版本自动切换，无文件时显示通用两版本对比提示

### V1.3.5

USPEX 10.6 兼容 — 自适应未知参数列

- USPEX 10.6 允许用户自定义额外物性列（如 `Property_X`），这些字段可能同时出现在 Individuals 和 Pareto_ranking 文件中。本版本全面适配此类未知列
- **Pareto 模块自动启用**：当上传的 Pareto_ranking 文件包含数据（即使 `Parameters.txt` 的 `optType` 仅为 `1`），自动将系统类型识别为多目标优化，侧边栏显示 Pareto Front 入口
- **Filter 页面自适应**：数值字段列表动态包含所有 `extraProps` 键（如 `Property_X`、`Thick`、`Surf_area` 等），支持条件筛选、排序、自定义文件名段。修复 `applyCondition` 直接访问 `s[field]` 导致 extraProps 字段永远返回 false 的 bug —— 新增 `getStructureValue()` 优先查直接属性、fallback 到 `s.extraProps`
- **Compare 页面自适应**：新增 Dynamic Extra Properties 对比区块，自动展示所有 `extraProps` 字段；补全此前遗漏的 Pugh's ratio（`pughRatio`）
- **CSV / ZIP 导出自适应**：`buildExportFilename` 中自定义字段名段同步支持 extraProps fallback
- **Filter 字段补全**：`eForm`、`eHullRecons`（变组分）、`enthalpyTotal` 加入可选字段列表

### V1.3.4

Explorer — 全字段 Δ 值自动计算

- Δ 值统一通过 `structureMap` 查找父代结构后计算差值（子代 − 父代），取代原先 `deltaE` 直接用 `parentEnthalpy`、`deltaObj` 取父代均值的方式。父代不在当前数据集中的结构返回 `undefined`

### V1.3.3

解析器重构 — 二维结构预测适配

- 重构 Individuals 解析器：改为基于表头列名映射（`headerUtils.ts`）按名取值，不再依赖硬编码列顺序。可灵活适配 3D bulk、2D、以及未来任意变体格式
- 重构 extended_convex_hull 解析器：同样使用表头列名映射，按名提取 Enthalpies / Volumes / Thickness / Fitness / SYMM / X / Y 等字段
- 新增二维结构搜索（2D structure search）文件支持：自动识别并正确解析 Individuals 中的 Thick (Å)、Surf_area (Å²)、Spec_surf_area (m²/g)、Fitness 列，以及 extended_convex_hull 中的 Thickness 列，存入 extraProps 动态列
- 修复 Hull Workshop 三元相图中多组数据 EA ID 碰撞导致点击/悬停/导出结构错误的 bug：`TernaryHullInput` 新增 `_mergeSeq` 字段贯穿 hull 去重流程，`structureMap` 改用唯一序列号作为 key

### V1.3.2

凸包工作台 — 结构查看与标记修复

- 修复 Hull Workshop 图表点击点查看 JSmol 时匹配到当前项目结构而非组内结构的 bug：图表组件新增 `onStructureClick` 回调，点击时传递完整 `Structure` 对象，`StructureViewerModal` 优先使用直接传入的结构数据
- 修复多组数据中出现相同 EA ID 时点击查找错误的 bug：合并时为每个结构分配唯一 `_mergeSeq`，图表 `customdata` 与点击查找均基于该字段区分
- 修复 Mark on Chart by EA ID 在 Hull Workshop 中不显示的 bug：将五角星覆盖轨迹拆分为 `tagOverlayTraces`（受 `showTags` 控制）和 `eaOverlayTraces`（始终生效）
- 修复 `@import` 顺序导致的 PostCSS 警告：Google Fonts 改为 `index.html` 中 `<link>` 加载

### V1.3.1

凸包工作台 — 修复与优化

- 修复 Tag 星标标记（`overlayTraces`）不受 `showTags` 控制、泄漏到 Hull Workshop 的问题
- 全局 Plotly 图表图例底色从不透明白底改为 60% 透明

### V1.3.0

凸包工作台（Hull Workshop）— 全新模块

- 新增 **Hull Workshop** 模块：多数据组合并构建统一凸包
  - 从当前项目导入 / 从已保存项目多选导入 / 上传 JSON 文件，每个来源成为独立数据组
  - 纯几何凸包算法：基于所有可见结构的 (composition, eForm) 计算，不依赖 USPEX 原始 fitness 字段
  - 支持二元、三元、定组分三种体系模式
  - 手动添加结构：输入化学式 + 焓值，即时测试结构是否扩展（降低）凸包；若扩展，图表以实线显示新凸包、虚线显示旧凸包，全部结构在新凸包下重算 energy above hull
  - 数据组可见性切换、重命名、删除
  - 导出 CSV（含 Group 列，方便外部工具绘图）和 JSON（完整结构数据保存与重新导入）
  - 工作区数据按项目隔离，持久化于 IndexedDB

凸包重构 — E_form 与 E_hull 独立计算

- 新增 `src/lib/convexHullReconstruction.ts` 模块，基于 USPEX 凸包几何（fitness=0 结构）独立重算形成焓和凸包距离
- 新增 **E_form (Recons)** 列：自动提取端元结构参考化学势，计算 formation enthalpy (eV/atom)
- 新增 **E_hull (Recons)** 列：基于 E_form 重算的凸包距离 (eV/atom)，二元用竖直距离插值，三元用重心坐标插值
- 两列仅在变组分系统显示，未收敛结构显示 `—`；Explorer / BetaExplorer 轴选项中同步新增
- 修复 E_form 轴选项因 `>= 0` 过滤导致合法负形成焓不显示的 bug

### V1.2.3

全局 — Header 工具栏优化

- 新增 **Cite USPEX** 按钮（书本图标）：点击弹出 Popover 面板，展示 USPEX 四类引用文献（General / Variable Composition / Random Topological / Multi-Objective Pareto），附"复制全部"按钮与 USPEX 官网链接
- 引用数据独立存放于 `src/utils/citations.ts`，后续更新引用只需编辑该文件
- Contact 按钮悬停提示改为"联系作者" / "Contact Author"（跟随语言切换）
- 调整 Header 右侧按钮顺序为：Upload → Language → Cite → Contact → Help

上传页面 — 一键打包命令与压缩包上传

- 新增**一键打包命令**卡片（QuickPackCommand）：展示在集群上打包 USPEX 结果目录的 shell 命令，点击一键复制，中英双语
- 新增**压缩包上传**支持：拖入单个 .tar.gz / .tgz / .zip 文件时自动解压并提取内部文件，无需手动解压

### V1.2.2

上传页面 — 示例数据与界面压缩

- 新增**加载示例数据**功能：点击上传页横幅中的"Load Sample Data"按钮，可一键加载内置演示项目（A-B-C 三元体系，50 GPa），无需上传任何文件即可体验全部功能

### V1.2.1

Overview 页面 — 计算参数卡片与数据完整性检查

- 新增**计算参数卡片**：展示元素组合、体系类型、优化类型、组分模式、外部压力、计算类型代码
- 新增**数据完整性卡片**：
  - 文件解析状态表（每个文件 ✓ / ✗ / —），区分核心文件、条件文件与可选文件
  - POSCAR 覆盖率与 Origin 覆盖率进度条，绿/黄/红三色编码
  - Pickup（续算）运行自动检测：解析 `Parameters.txt` 中的 pickUpGen 和 pickUpFolder 参数，出现时显示黄色警告横幅
  - 解析警告（`parseWarnings`）以信息横幅展示
- 空间群分布改为 **Plotly 环形饼图**（top 10 + Others 聚合），`sort: false` 确保 Others 始终排在图注末尾
- 移除统计卡片中的最低焓值与 Max ML 弹性模量

### V1.2.0

全局 — 页面引导与状态持久化

- 新增右侧帮助抽屉（Page Guide）：点击顶部工具栏 `?` 可打开或关闭，显示当前页面的功能介绍与基础知识
- 帮助抽屉状态改为全局项目级持久化：在一个页面关闭后，其他页面也会默认关闭；重新打开后会全局生效
- 已完成完整引导的页面包括：Data Table、Convex Hull / Energy Ranking、Pareto Front、Explorer、HV Tracker、Filter & Export

全局 — UI 状态持久化修复

- 将 DataTable 页面筛选条件（`tableFilters`、`tableFilterGroups`）重新纳入 localStorage 持久化
- 新增 DataTable 页面的全局搜索词（`tableGlobalFilter`）和标签筛选（`tableSelectedTag`）持久化，避免切换页面后丢失
- 切换项目时会自动清空上述项目相关筛选状态

Explorer 页面

- 修复点击 Play 或导出 GIF 时，若颜色范围上限已处于最大值则无动画的问题；现在会自动将上限重置到最小值，从头开始播放

### V1.1.3

各图表 — 导出数据（Export Data）

- 每张图右上角新增 **Export Data** 按钮，将当前图中可见数据导出为 Origin 兼容的 CSV 文件
- 导出内容严格遵循当前筛选状态：
  - **Energy Ranking**：仅导出 Top N 结构（由 slider 控制），按 Rank 排序，含 EA_ID / Formula / SpaceGroup / Generation / Origin / Enthalpy / Fitness
  - **Binary Hull**：双 section 格式，Section 1 含所有可见点（Stable + Unstable，含 Type 列），Section 2 为凸包折线顶点，均受 Fitness max slider 筛选
  - **Ternary Hull**：导出组分分数（x_A / x_B / x_C）而非笛卡尔坐标，Origin Ternary Plot 可直接使用，受 Fitness max slider 筛选
  - **Pareto**：仅导出用户选中的前沿，**Wide Format**（每条 Front 独立两列：`Front1_Fitness / Front1_[ObjName] / ...`），按 Fitness 排序，Origin 可直接选列画多条折线
  - **Explorer**：导出颜色范围内可见点，列名动态跟随 X / Y / Color 轴选择
  - **HV Tracker — Scatter**：仅导出 ≤ numFronts 的前沿，**Wide Format**，按 Front 升序、X 轴方向排序
  - **HV Tracker — HV Convergence**：**Wide Format**（每条 Front 独立两列：`Front1_Generation / Front1_Hypervolume / ...`），Origin 可直接画多条收敛曲线
- Wide Format 不同 Front 点数不同时，较短的列自动补空格
- CSV 文件名包含元素、图类型、当前筛选参数等信息（如 `Fe-B_pareto_front1-3.csv`）
- 所有 CSV 带 UTF-8 BOM，Origin 拖入即可识别

### V1.1.2

Filter 页面 — 导出文件命名

- 新增自定义命名段：在 ZIP 导出的命名规则区域下方，用户可添加任意数量的自定义段
- 每个自定义段由一个前缀文本框（如 `Gen`）和一个字段下拉框（动态列出当前数据中所有数值字段）组成
- 字段下拉列表与排序字段保持一致，随数据自动包含 Pareto、ML 弹性模量、指纹等可选字段
- 整数字段（generation、spaceGroup 等）直接输出整数，浮点字段保留 3 位小数
- 自定义段追加在预设段之后，文件名预览实时更新
- 设置持久化到 localStorage，刷新后恢复

### V1.1.1

Filter & DataTable 筛选功能

- 新增 OR 条件组支持：筛选条件改为"组内 AND，组间 OR"结构
- 新增"+ OR"按钮，点击后将当前条件作为新 OR 分支添加
- 新增"Append to group"按钮，可将后续条件追加到指定组（AND 关系）

### V1.1.0

新增 HV Tracker 模块（侧边栏 → HV Tracker）

- 在任意两个数值轴上自动计算 Pareto front（Layer Classification 算法，与 USPEX 内部一致）
- 每个轴独立设置优化方向（最小化 / 最大化）
- 散点图按 Pareto front 编号着色，只显示前 N 层（N 可调），各层连线并用对应颜色高透明填充标准 2D hypervolume staircase 区域
- 参考点（Reference Point）默认自动计算（nadir + 10% range，对负值正确处理），支持手动覆盖
- HV vs Generation 图（上方）：对 cumulative archive（generation ≤ g）做 Layer Classification，分别绘制各 Pareto front 的 hypervolume 随代数的变化曲线，每条 front 独立一根线

### V1.0.6
Explorer 页面
- 新增边际分布图（Marginal Histogram + KDE）：X/Y 轴各有独立开关（`∫ dist` 按钮），挂在对应轴选择器后面
- X 轴分布图出现在主图上方，Y 轴分布图出现在主图右侧，坐标轴通过 Plotly subplot `matches` 机制天然对齐
- 直方图叠加 KDE 曲线（Silverman bandwidth 自动计算），`histnorm: 'probability density'` 保证单位一致
- bins 数量可调（5–200，默认 30），任意一个边际图开启时显示
- 新增 `≠0` 按钮：开启后边际图自动排除零值（适用于"无关数据记录为 0"的字段）
- 轴范围输入框同步作用于边际图统计：设置 X/Y 范围后，分布图只统计范围内的数据
- 两种过滤可叠加使用，`n=xxx` 实时显示参与统计的数据量

修复
- 修复同一计算跑多次时，新项目覆盖 Recent Projects 中已有记录的 bug：项目 ID 改为首次创建时生成的唯一值（timestamp + 随机串），后续自动保存复用同一 ID

### V1.0.5

全局
- 英文与数字字体改为 Times New Roman

Explorer 页面
- Color 滑块新增自动播放：上限从当前位置步进至 dataMax，下限固定不动
- 播放参数可调：步进量（step）和帧率（fps）均可在滑块旁实时修改
- 新增 GIF 导出：将播放过程逐帧截图并合成动图下载，绕开 React 状态异步问题，直接用 `Plotly.react()` 强制渲染每帧

Filter 页面
- CSV 导出从 9 列扩展为完整字段：新增 Composition、ParentIDs、Tags、Notes
- CSV 列动态生成：与 Table 页保持一致，仅当数据中实际存在时才输出 Pareto（ParetoFront、ExtraProps）、ML 弹性模量（Bulk/Shear/Young/Poisson/Pugh/Vickers/FractureToughness）、指纹参数（Q_Entropy/A_Order/S_Order）列

### V1.0.4

Explorer 页面
- 新增 ΔE 字段：`E_child - parentEnthalpy`，无父母的结构自动隐藏
- 新增 ΔObj 字段：对每个 extraProps 键各生成一个，采用双亲均值方案 `obj_child - avg(obj_parent1, obj_parent2)`
- Color By 数值字段时，新增双滑块过滤器：拖动范围排除区间外的点，色带本身固定不变
- X/Y 轴范围输入框、Mark on Chart 移至图表下方

凸包图 / Pareto / Energy Ranking
- Mark on Chart 面板统一移至图表下方

### V1.0.3

上传页面
- 项目命名改为自动生成：格式为 `元素-calculationType-压强GPa[-后缀]`，例如 `Li-H-300-100GPa-Tc`
- 输入框由"项目名称"改为可选后缀，不填则自动生成不带后缀的名称，不再强制要求用户命名
- 强制要求上传全部 4 个核心文件（Individuals、origin、Parameters.txt、gatheredPOSCARS）才能开始分析

解析器
- `parametersParser` 新增解析 `ExternalPressure` 字段（单位 GPa）
- `SystemInfo` 新增 `calculationType` 和 `externalPressure` 字段
- 移除无 Parameters.txt 时的元素占位符 fallback 逻辑（`Elem1`, `Elem2`...）

修复
- 修复新建项目后不出现在"最近的项目"列表的 bug：`setProjectName` 现在会在设置名称后触发一次自动保存

### V1.0.2
Filter 页面
筛选字段动态生成：根据当前项目实际数据决定显示哪些字段（多目标参数、弹性模量、指纹等），不再硬编码全量列表

数据表格
筛选条件持久化：切换页面后返回，已添加的筛选条件不再丢失

全局
修复单目标优化项目出现 Fitness-Individuals 列的 bug：`detectSecondObjective` 将 `Fitness` 列正确识别为标准列

所有图表（凸胞图、三元相图、能量排名、Explorer、Pareto）
新增 Mark 标记功能：在每个图表页面顶部显示 MarkPanel 控制面板
- 按标签标记：点击标签按钮（Candidate / To Verify / Excluded / Bookmarked 及自定义标签），对应结构以该标签颜色的五角星覆盖显示在图上
- 按 EA ID 标记：输入框支持 "EA1, EA5, EA10" 或 "1 5 10" 等格式，匹配结构以金色（#FFD700）五角星标出，实时显示匹配数量

### V1.0.1
Filter 页面
标签筛选重新设计：两个独立区域合并为单列表，点击循环切换三态（灰=忽略 / 绿✓=必须含有 / 红✗=必须排除）
修复排除标签在加入数值筛选条件后失效的 bug
筛选条件、导出格式、排序方式等设置切换页面后不再丢失
加入元素占比的筛选

数据表格
前四列（ID、化学式、标签、操作）固定冻结，横向滚动时始终可见
标签下拉框和备注弹框改为浮层渲染，不再被表格遮挡，并自动避开屏幕边缘
筛选功能扩展：数字列覆盖所有可用字段（弹性模量、指纹等）；新增文字列筛选（化学式/来源），支持多选 + 包含/不包含/等于/不等于
界面文字全面支持中英文切换

全局
Explorer 轴选择、Pareto 前沿选择、表格排序状态切换页面后均不再重置
所有页面 UI 状态自动保存到本地，刷新浏览器后恢复
